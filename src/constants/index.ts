// Common constants used across the app
export const API_ENDPOINTS = {
  AUTH: '/auth',
  USERS: '/users',
  // Add more endpoints as needed
};

export const STORAGE_KEYS = {
  AUTH_TOKEN: 'auth_token',
  USER_DATA: 'user_data',  // Add more storage keys as needed
};
